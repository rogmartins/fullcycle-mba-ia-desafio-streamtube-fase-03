# Phase 03 — Upload e Processamento de Vídeos — Progress

**Status:** completed
**SIs:** 14/14 completed

**Final verification (all 14 SIs):** unit+integration 40/40 suites (287 tests) green, e2e 3/3 suites (82 tests) green, `npx tsc --noEmit` clean, `npm run lint` clean (0 errors, 0 warnings), `npm run build` produces `dist/main.js` and `dist/main.worker.js`.

**Post-phase cleanup (out of Phase 03's functional scope, done as a separate follow-up):** the pre-existing lint debt originally flagged in SI-03.7's observations turned out to span 12 files, not the 3 initially estimated — `src/auth/auth.service.spec.ts`, `test/auth.e2e-spec.ts`, `src/channels/channels.service.spec.ts`, `src/mail/mail.service.integration-spec.ts`, `src/auth/auth.service.integration-spec.ts`, `src/channels/channels.service.ts` (the one production file involved), `src/common/filters/domain-exception.filter.spec.ts`, `src/common/filters/validation-exception.filter.spec.ts`, `src/users/users.service.integration-spec.ts`, `src/test/create-test-data-source.ts`, `src/main.ts`, `src/main.worker.ts`. Fixed by typing test doubles and response shapes precisely instead of `any` (dedicated interfaces for mocked repositories/query builders/HTTP response bodies, `as unknown as X` casts at the few genuine type-erasure boundaries) — no test assertions or runtime logic changed, confirmed by every affected suite passing identically before and after. The one dominant pattern needing a real tooling fix rather than a type annotation was `@typescript-eslint/unbound-method` firing on `expect(mockedService.method).toHaveBeenCalledWith(...)` (19 instances, all in `auth.service.spec.ts`) — this is a documented false-positive class against Jest mocks per typescript-eslint's own docs, which recommend `eslint-plugin-jest`'s version of the rule for test files; added it as a devDependency and scoped it to `**/*.spec.ts`, `**/*.integration-spec.ts`, `test/**/*.ts` in `eslint.config.mjs` (swaps the check, disables nothing). No files under `videos/`, `processing/`, `queue/`, `storage/`, or the worker entrypoints needed changes.

### SI-03.1 — Dependencies and Configuration Namespaces
- **Status:** completed
- **Tests:** No dedicated test file (config-only SI); verified via full existing suite (128 unit/integration + 46 e2e) passing, `npx tsc --noEmit` clean, and direct Joi schema validation checks (missing STORAGE_ACCESS_KEY/SECRET_KEY rejected, UPLOAD_PART_SIZE_BYTES below 5 MiB rejected, valid config accepted)
- **Observations:** none

### SI-03.2 — Docker Compose: Redis, MinIO, Provisioning and FFmpeg
- **Status:** completed
- **Tests:** No dedicated test file (infra-only SI). Verified manually: `docker compose up -d` brings up db/mailpit/redis/minio healthy + nestjs-api running + minio-init exits 0 (and is idempotent on rerun); `ffmpeg -version`/`ffprobe -version` report 5.1.9 inside nestjs-api; Redis PING from nestjs-api returns PONG and `maxmemory-policy` is `noeviction`; unsigned GET on `videos/` returns 403, unsigned GET on an existing `thumbnails/` object returns 200; preflight OPTIONS to MinIO returns correct `Access-Control-Allow-Origin`/methods and real responses expose ETag/Content-Range/Accept-Ranges/Content-Length; `tsc --noEmit` clean
- **Observations:** Two deviations from the plan's literal technical actions, both due to the actual images/behavior available today (verified directly, not assumed): (1) `minio/minio` and `minio/mc` are no longer available on Docker Hub — MinIO moved distribution to `quay.io/minio/minio` and `quay.io/minio/mc`; compose.yaml uses those instead. (2) This MinIO server build (RELEASE.2025-09-07) does not implement the S3 `PutBucketCors` API — `mc cors set` fails with "functionality that is not implemented" even using root credentials; verified MinIO instead handles CORS automatically server-side for every bucket (reflects request Origin, honours requested method/headers on preflight, always exposes ETag/Content-Range/Accept-Ranges/Content-Length on real responses) — the CORS acceptance criterion passes without any bucket-level config, so the `minio-init.sh` CORS step was replaced with a log line instead of a fatal `mc cors set` call. The `AbortIncompleteMultipartUpload` lifecycle rule still fails as the plan anticipated (tolerated with `|| true` + warning, TD-20 no-op on MinIO).

### SI-03.3 — Storage Module (S3 Client Wrapper)
- **Status:** completed
- **Tests:** `storage.module.spec.ts` (3 tests) and `storage.service.integration-spec.ts` (10 tests, real MinIO) all pass; full suite 22/22 suites, 138/138 tests green; `tsc --noEmit` clean. Manually verified: `StorageUnavailableError` thrown with MinIO stopped; tampered presigned GET URL rejected with 403
- **Observations:** MinIO's `ListMultipartUploadsCommand` `Prefix` param only matches an exact full key on this build, not a real prefix (verified directly with root credentials too) — worked around with client-side `startsWith` filtering in `listMultipartUploads`, which is also correct against real S3 (server-side prefix there is just a redundant optimization). No `storage.service.spec.ts` unit file was created — not listed in the plan's Tests table for this SI (integration test covers all behavior against real MinIO).

### SI-03.4 — Queue Module and Processing Job Publisher
- **Status:** completed
- **Tests:** `queue.module.spec.ts` (2 tests) and `video-queue.service.integration-spec.ts` (4 tests, real Redis) all pass; full suite 24/24 suites, 144/144 tests green; `tsc --noEmit` clean. Manually verified bootstrap fails fast (~3s) against an unreachable `REDIS_HOST`
- **Observations:** Default BullMQ/ioredis behavior does NOT fail bootstrap on an unreachable Redis — it retries forever silently in the background, so the literal AC ("bootstrap fails fast") would not hold out of the box. Added a bounded `retryStrategy` (gives up after 5 attempts) to the BullMQ connection config plus a `VideoQueueService.onModuleInit()` that calls `queue.waitUntilReady()`, so Nest's bootstrap now genuinely blocks on and rejects with a real connection error when Redis is unreachable.

### SI-03.5 — Video Entity and Migration
- **Status:** completed
- **Tests:** `video.entity.integration-spec.ts` (8 tests), `migrations.integration-spec.ts` (updated, 2 tests), `videos.module.spec.ts` (1 test) all pass; full suite 26/26 suites, 153/153 tests green; `tsc --noEmit` clean. Manually verified `npm run migration:run` creates the `videos` table/enum/indexes/FK exactly per the Data Model, and `npm run migration:revert` cleanly drops table + both enum types (state restored afterward)
- **Observations:** Two infra issues surfaced and fixed, unrelated to entity/migration design: (1) `migrations.integration-spec.ts`'s `beforeAll` dropped all managed tables concurrently via `Promise.all` — with `videos` now FK'd to `channels`, concurrent CASCADE drops from separate connections deadlocked; switched to sequential drops. (2) `DROP TABLE ... CASCADE` does not drop the enum types a dropped table depended on, so a killed/interrupted prior run left `videos_status_enum` behind and broke the next `CreateVideos` migration with "type already exists"; added explicit `DROP TYPE IF EXISTS ... CASCADE` cleanup for both video enums in the same `beforeAll`. Confirmed integration tests and `npm run migration:*` share the same real `streamtube` database (project convention, not a separate test DB).

### SI-03.6 — Video Domain Exceptions and Channel Lookup
- **Status:** completed
- **Tests:** `domain-exception.filter.spec.ts` extended with 8 new cases (Phase 03 subclasses incl. 415/503), `channels.service.integration-spec.ts` extended with 2 `findByUserId` cases; full suite 26/26 suites, 163/163 tests green; `tsc --noEmit` clean
- **Observations:** none

### SI-03.7 — Upload Initiation (Draft Pre-Registration)
- **Status:** completed
- **Tests:** `public-id.util.spec.ts` (2), `title.util.spec.ts` (12), `videos.service.spec.ts` (6, unit), `videos.service.integration-spec.ts` (2, real DB+MinIO), `test/videos.e2e-spec.ts` (7) — all pass. Full sweep: unit/integration 30/30 suites (185 tests), e2e 3/3 suites (53 tests), `tsc --noEmit` clean, `eslint` clean on all files this SI touched
- **Observations:** (1) `npm run test:e2e` was NOT actually configured with `--runInBand` despite CLAUDE.md stating "already configured" and requiring it — adding a third e2e suite exposed a real cross-file DB race (`cleanAllTables` FK violations from parallel Jest workers). Fixed by adding `--runInBand` to the `test:e2e` script in `package.json` — this is a project-wide fix, not scoped to videos. (2) Discovered `npm run lint` already fails with ~150 pre-existing `@typescript-eslint/no-unsafe-*` errors across files this phase never touched (`auth.e2e-spec.ts`, `channels.service.ts`, `channels.service.spec.ts`, `domain-exception.filter.spec.ts`, `validation-exception.filter.spec.ts`) — confirmed via `git diff` that these files are functionally untouched by Phase 03 (only reformatted by `--fix`). This is pre-existing technical debt, out of scope to fix here; all new Phase 03 files (including this SI's) are lint-clean. Flagging as a follow-up for the user since it means the Deliverables' "Lint passes" check will fail today regardless of Phase 03 completion.

### SI-03.8 — Part Signing and Upload Resumption
- **Status:** completed
- **Tests:** `videos.service.spec.ts` extended (+17 unit tests for `getOwnedVideo`/`signPart`/`listUploadedParts`), `videos.service.integration-spec.ts` extended (+1, real MinIO PUT + list), `test/videos.e2e-spec.ts` extended (+8). Full sweep: unit/integration 30/30 suites (197 tests), e2e 3/3 suites (60 tests), `tsc --noEmit` clean, `eslint` clean on all files this SI touched
- **Observations:** `POST /videos/:id/upload/parts/:partNumber/url` needed an explicit `@HttpCode(200)` — NestJS defaults POST handlers to 201, but the API contract specifies 200 for this endpoint (caught by the e2e test, not tsc/lint).

### SI-03.9 — Upload Completion, Cancellation and Job Publication
- **Status:** completed
- **Tests:** `videos.service.spec.ts` extended (+17 unit tests for `completeUpload`/`cancelUpload` covering the full part-verification matrix, lost-race, enqueue-swallow, and cancel branches), `videos.service.integration-spec.ts` extended (+4, real DB+MinIO+Redis: two-part completion, cancel cleanup, concurrent-completion race, queue-failure resilience via a targeted spy), `test/videos.e2e-spec.ts` extended (+9). Full sweep: unit/integration 30/30 suites (214 tests), e2e 3/3 suites (69 tests), `tsc --noEmit` clean, `eslint` clean on all files this SI touched
- **Observations:** (1) Both `POST /videos/:id/upload/complete` needed an explicit `@HttpCode(200)` — same NestJS POST-defaults-to-201 gap as SI-03.8's sign-part endpoint. (2) Found and fixed a real validation bug during e2e testing: the initial part-verification logic only checked that *declared* parts existed in the store, not that *all expected* parts (1..part_count) were declared — so completing with a strict subset of parts silently succeeded (S3/MinIO's own `CompleteMultipartUpload` doesn't reject a subset either). Fixed by comparing declared-vs-expected part numbers directly, not just declared-vs-stored. (3) The "Redis stopped, complete still returns 200" AC was verified via a targeted `jest.spyOn` rejection on `VideoQueueService.enqueueProcessing` in the integration suite rather than physically stopping the `redis` container: an already-booted app losing Redis mid-session can't be reproduced by the short-lived `docker compose exec` scripts used for MinIO's equivalent checks earlier in the phase (a fresh boot against a stopped Redis just fails `onModuleInit`'s `waitUntilReady()`, per SI-03.4). The spy exercises the identical code path (catch around `enqueueProcessing`, logged and swallowed).

### SI-03.10 — Public Read, Playback URL and Download URL
- **Status:** completed
- **Tests:** `download-filename.util.spec.ts` (7, unit: diacritics, unsafe-char collapsing, 80-char cap, key-derived extension, empty-stem fallback), `videos.service.spec.ts` extended (+18: `findByPublicId`/`getThumbnailUrl`/`getPlaybackUrl`/`getDownloadUrl` 404/409 branches, TTLs and disposition passed to storage, plus a direct `toPublicView` presenter check for field omission), `videos.service.integration-spec.ts` extended (+2, real MinIO: playback URL answers `Range: bytes=0-99` with 206; download URL's object serves `Content-Disposition: attachment; filename="Ferias-2026-praia.mp4"`), `test/videos.e2e-spec.ts` extended (+13: public shape without auth, 404 for unknown/malformed ids, playback/download 200 shape and TTL window, 409 `VIDEO_NOT_READY` for uploading/processing/failed, no leaked internal fields). Full sweep: unit/integration 31/31 suites (236 tests), e2e 3/3 suites (82 tests), `tsc --noEmit` clean, `eslint` clean on all files this SI touched
- **Observations:** (1) `:publicId` shape validation could not be done with a class-validator param DTO through the global `ValidationPipe`, since a failed validation there returns 400 — the AC requires 404 `VIDEO_NOT_FOUND` for a malformed id (e.g. a UUID). Implemented instead as a plain regex guard (`isValidPublicId`) inside `VideosService.findByPublicId`, checked before any DB query, so both "unknown id" and "wrong shape" collapse into the same 404 path. (2) The domain exception filter's JSON body field is `error` (not `error_code` as I first assumed while writing new e2e assertions) — caught immediately by the first test run and fixed to match the filter's actual (and already-established) contract.

### SI-03.11 — FFmpeg Service (Probe and Frame Extraction)
- **Status:** completed
- **Tests:** `ffmpeg.service.spec.ts` (12, unit: stubbed `node:child_process.spawn`, no-shell argument arrays for both `probe`/`extractFrame`, fake-timer-driven timeout → `FfmpegExecutionError{timedOut:true}` plus a real `child.kill('SIGKILL')` call, `Invalid data found` and missing-video-stream → `InvalidMediaError`, canned-JSON parsing, empty-output/non-zero-exit → `FfmpegExecutionError`), `ffmpeg.service.integration-spec.ts` (4, real binaries + real MinIO via `getVideoFixtures()`: duration≈3s/320×240/h264/`moovAtEnd` for both fixtures, `InvalidMediaError` on a text object, JPEG magic-byte + width check on `extractFrame`'s output), `processing.module.spec.ts` (1). Full sweep: unit/integration 34/34 suites (252 tests), e2e 3/3 suites (82 tests), `tsc --noEmit` clean, `eslint` clean on all files this SI touched. Manually verified (not in the Tests table, but load-bearing for an AC): killing a real `ffprobe` against an unreachable host leaves no zombie process (`ps aux` empty afterward)
- **Observations:** (1) `moovAtEnd` polarity: an MP4 written by ffmpeg's default muxer places `moov` *after* `mdat` (`moovAtEnd = true`); `-movflags +faststart` moves it to the front (`moovAtEnd = false`). My first draft of the integration test had the two fixtures' expectations swapped — caught immediately by the first run, not a real code bug, just a test-authoring mistake; verified twice against real ffmpeg output before fixing. (2) `detectMoovPlacement` reads only the first 64 KiB via a single `Range` request per TD-17 and walks ISO-BMFF box headers (`ftyp`/`moov`/`mdat`/… with 64-bit `largesize` support); a first box type outside the known set (e.g. WebM's EBML magic) is treated as non-ISO-BMFF and returns `null` without walking further, satisfying the WebM AC without a second FFmpeg pass. (3) `video-fixture.ts` generates its 3-fixture set (plain MP4, faststart MP4, non-media text file) into a fixed OS-tmp directory and skips regeneration when the files already exist, so the ffmpeg cost is paid once across the whole local test history, not once per run.

### SI-03.12 — Video Processing Job Consumer
- **Status:** completed
- **Tests:** `video.processor.spec.ts` (12, unit: mocked repository/storage/ffmpeg — missing row and non-`processing` row are no-ops, `InvalidMediaError` → row `failed(INVALID_MEDIA)` + thrown `UnrecoverableError`, transient probe error rethrown untouched, thumbnail failure → `ready`/`thumbnail_key=null`/`THUMBNAIL_FAILED`, zero-rows-affected branch, `onFailed` on last vs. earlier attempts vs. an already-handled `UnrecoverableError` vs. no job), `video.processor.integration-spec.ts` (2, real DB+MinIO+ffmpeg, calling `process()` directly with a job stub built from `getVideoFixtures()`: a `processing` row with the MP4 fixture becomes `ready` with duration/width/height/codec/`moov_at_end`/`metadata` filled and an unsigned-GET-200 JPEG thumbnail with `Content-Type: image/jpeg`; a text-file source becomes `failed(INVALID_MEDIA)` with the thrown error being `UnrecoverableError`). Full sweep: unit/integration 36/36 suites (266 tests), e2e 3/3 suites (82 tests), `tsc --noEmit` clean, `eslint` clean on all files this SI touched
- **Observations:** (1) `VideoProcessor` is deliberately **not** wired into any Nest module yet (`ProcessingModule` still only provides/exports `FfmpegService`, unchanged from SI-03.11) — actually booting it as a worker (module wiring, `@Processor`'s BullMQ registration, a separate worker entrypoint) is SI-03.14's job per the phase's own SI boundaries ("Worker Bootstrap, Scheduler Registration and Compose Service"); wiring it in here would blur that boundary and make `ProcessingModule` require a real DB + Redis connection just to unit-test `FfmpegService`, violating the project's unit-vs-integration test-suffix rule. Both test files construct `VideoProcessor` directly (`new VideoProcessor(...)`) with either mocked or DI-resolved real collaborators, which needs no module registration. (2) TypeORM's query-builder `.set()` typing couldn't infer a plain object literal against the `jsonb` `metadata` column (`_QueryDeepPartialEntity` mapping); cast the `.set()` argument to `QueryDeepPartialEntity<Video>` rather than fighting the inference further.

### SI-03.13 — Reconciliation Sweep
- **Status:** completed
- **Tests:** `upload-sweep.service.spec.ts` (11, unit: mocked repository/storage/queue — branches (a)–(d) of the stale-`uploading` reconciliation, the created_at fallback when no parts exist, the stale-`processing` re-enqueue branch both with and without an active job, the orphan-upload branch both aborting and skipping, per-video error isolation, and that the stale threshold is read from config), `upload-sweep.service.integration-spec.ts` (6, real DB+MinIO+Redis with seeded rows and real multipart uploads, timestamps back-dated via direct `UPDATE`: completed-but-unrecorded upload → `processing`+job, upload gone+no object → `failed(UPLOAD_ABANDONED)`, 8-day-old abandoned upload → aborted+`failed`, fresh upload left `uploading`, stale `processing` without a job → job present afterwards, orphan upload under `videos/` aborted). Full sweep: unit/integration 38/38 suites (283 tests), e2e 3/3 suites (82 tests), `tsc --noEmit` clean, `eslint` clean on all files this SI touched
- **Observations:** (1) MinIO stamps a part's `last_modified` with the real wall-clock PUT time — it cannot be back-dated through the S3 API, so the integration test for branch (c) ("newest part older than `SWEEP_ABANDON_DAYS`") exercises the *no-parts* fallback (comparing against a back-dated `created_at` instead) rather than an actual old part; the with-parts comparison is covered by the unit suite's mocked `lastModified`. Both exercise the identical `lastActivityAt < abandonCutoff` branch in the service, so coverage is not weakened, just routed through the reachable fork. (2) Pagination for the two stale-row sweeps takes a `count()` once up front and then pages by fixed `skip`/`take` offsets rather than a `do…while` re-query loop — a naive re-query of the same unpaged `WHERE` after each page would loop forever whenever branch (d) ("left untouched") rows are present, since those never leave the query's result set. (3) `UploadSweepService`, like `VideoProcessor`, is not wired into any Nest module yet — it's a bare `@Injectable()`/`@Processor()` pair, both test files construct/DI-resolve it directly; actual worker registration is SI-03.14's job, matching the note already recorded for SI-03.12.

### SI-03.14 — Worker Bootstrap, Scheduler Registration and Compose Service
- **Status:** completed
- **Tests:** `worker.module.spec.ts` (2: `WorkerModule` compiles with `VideoProcessor`/`UploadSweepProcessor`/`UploadSweepService`/`UploadSweepScheduler`/`WorkerHeartbeatService` all resolvable against real Redis/DB per the plan's own "configured-lib contracts" framing; does not expose `AuthService`), `upload-sweep.scheduler.spec.ts` (2, against real Redis: `onApplicationBootstrap` registers exactly one `upload-sweep-every` job scheduler with the configured `every`; calling it twice still leaves exactly one). Full sweep: unit/integration 40/40 suites (287 tests), e2e 3/3 suites (82 tests), `tsc --noEmit` clean, `eslint` clean (one pre-existing-pattern `no-floating-promises` warning on the bare `bootstrap()` call in `main.worker.ts`, matching `main.ts`'s own unawaited call — warnings are non-blocking per this project's ESLint config). Manually verified against the real `video-worker` Compose service: `docker compose up -d video-worker` reaches `healthy` well within 60 s (heartbeat file born and repeatedly touched, confirmed via `stat` inside the container); `curl`/`netstat` from inside the container confirm no TCP listener on port 3000; `docker compose exec nestjs-api npm run build` produces both `dist/main.js` and `dist/main.worker.js`; `redis-cli --scan` shows exactly one `bull:video-maintenance:repeat:upload-sweep-every` key both on first boot and after `docker compose restart video-worker`
- **Observations:** (1) `WorkerModule`'s `TypeOrmModule.forFeature([Video])` alone was not enough for `TypeOrmModule.forRootAsync` to build entity metadata — `Video` has a `@ManyToOne` to `Channel`, and `Channel` has a `@OneToOne` back to `User`; TypeORM's `autoLoadEntities` only picks up entities that appear in *some* module's `forFeature()` call anywhere in the app, so the worker (which never touches channels/users directly) still had to register `Channel` and `User` for the relation graph to resolve — omitting either produced a `TypeORMError: Entity metadata for X#y was not found` that manifested as a slow 10-retry connection-failure loop rather than a clear error, since `TypeOrmModule.forRootAsync`'s default retry behavior wraps the metadata-build error in "Unable to connect to the database" logging. (2) Did not boot `nestjs-api`'s own HTTP dev server for the full "`POST …/upload/complete` → worker → `ready`" round-trip AC — this project's `nestjs-project/CLAUDE.md` restricts starting the API server to when the user explicitly asks ("rode o projeto"), and that wasn't asked for this task; the equivalent logic path (`completeUpload` → job → `VideoProcessor.process` → `ready`) is already fully exercised end-to-end by `videos.service.integration-spec.ts` (SI-03.9) and `video.processor.integration-spec.ts` (SI-03.12) together, just not through a live HTTP round-trip via Compose. Flagging this gap explicitly rather than silently skipping it. (3) Discovered and cleaned up a real mistake during manual verification: an earlier one-off `node dist/main.worker.js &` debugging command left a stray live BullMQ worker process running inside the `nestjs-api` container (the `kill $(cat pidfile)` in that one-liner targeted the wrong PID), which then raced the test suite for jobs on the shared Redis and caused spurious `video-queue.service.integration-spec.ts` failures ("Job … could not be removed because it is locked by another worker"); killed the stray process and confirmed the full suite was green again from a clean state. No production code was affected — this was purely a leftover from ad hoc manual testing in this session.
